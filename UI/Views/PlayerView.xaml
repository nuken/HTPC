<UserControl x:Class="HTPC.UI.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Black" Foreground="White">

    <UserControl.Resources>
        <Style x:Key="TransportButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#00A4EF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="RootGrid">
        
        <Grid x:Name="VideoSurface" Background="Black"/>

        <Popup x:Name="ControlsPopup" AllowsTransparency="True" Placement="Relative" PlacementTarget="{Binding ElementName=RootGrid}">
            
            <Grid x:Name="ControlsOverlay" Background="#01000000" MouseMove="ControlsOverlay_MouseMove">
                
                <Border x:Name="TopBar" VerticalAlignment="Top" Background="#CC0F0F0F" Padding="20" CornerRadius="0,0,16,16" Margin="40,0,40,0" Height="80">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" Direction="270" ShadowDepth="4" BlurRadius="12" Opacity="0.5"/>
                    </Border.Effect>
                    <Grid>
                        <Button Content="🔙" HorizontalAlignment="Left" Style="{StaticResource TransportButtonStyle}" Click="Back_Click" FontSize="28"/>
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock x:Name="ShowTitleText" Text="" Foreground="#AAAAAA" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="MediaTitleText" Text="Loading..." Foreground="White" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border x:Name="BottomBar" VerticalAlignment="Top" Background="#CC0F0F0F" Padding="30,20" CornerRadius="16" Height="120">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" Direction="90" ShadowDepth="4" BlurRadius="12" Opacity="0.5"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="CurrentTimeText" Grid.Column="0" Text="00:00:00" Foreground="White" FontSize="14" Margin="0,0,15,0" VerticalAlignment="Center"/>
                            
                            <Slider x:Name="TimelineSlider" Grid.Column="1" VerticalAlignment="Center" Cursor="Hand" 
                                    Minimum="0" Maximum="100" Value="0" IsMoveToPointEnabled="True"
                                    Thumb.DragStarted="Timeline_DragStarted" Thumb.DragCompleted="Timeline_DragCompleted"/>
                            
                            <TextBlock x:Name="RemainingTimeText" Grid.Column="2" Text="-00:00:00" Foreground="#AAAAAA" FontSize="14" Margin="15,0,0,0" VerticalAlignment="Center"/>
                        </Grid>

                        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Button Content="⏮" Style="{StaticResource TransportButtonStyle}" ToolTip="Skip Backward"/>
                            <Button x:Name="PlayPauseButton" Content="⏸" Style="{StaticResource TransportButtonStyle}" FontSize="36" Margin="20,0" Click="PlayPause_Click"/>
                            <Button Content="⏭" Style="{StaticResource TransportButtonStyle}" ToolTip="Skip Forward"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Popup>
    </Grid>
</UserControl>